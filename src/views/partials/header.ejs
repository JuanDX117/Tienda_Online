<!-- Navbar -->
<nav class="navbar fixed-top navbar-expand-lg navbar-light" style="background-color: #333; color: #fff;">
    <div class="container">

        <!-- Brand -->
        <a class="navbar-brand waves-effect" href="/">
            <strong class="blue-text">J.E.F.S.</strong>
        </a>

        <!-- Collapse -->
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Links -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">

            <!-- Left -->
            <ul class="navbar-nav mr-auto align-items-center">
                <li class="nav-item <%= locals.active == '' ? 'active':'' %> ">
                    <a class="nav-link waves-effect" href="/" style="color: #fff;">Inicio
                        <% if (locals.active=='/' ) { %>

                            <span class="sr-only">(current)</span>
                            <% } %>
                    </a>
                </li>
                <li class="nav-item <%= locals.active == 'products' ? 'active':'' %>">
                    <a class="nav-link waves-effect" href="/products" style="color: #fff;">Productos</a>
                    <% if (locals.active=='products' ) { %>

                        <span class="sr-only">(current)</span>
                        <% } %>
                </li>

                <% if (locals.userFound) { %>
                    <li class="nav-item  <%= locals.active == 'profile' ? 'active':'' %>">
                        <a class="nav-link waves-effect" href="/profile" style="color: #fff;">Mi Cuenta </a>
                    </li>
                    <li class="nav-item">
                        <form action="/logout" method="POST">
                            <button class="btn-link btn py-2 px-3 m-0 nav-link waves-effect"><i
                                    class="fas fa-sign-out-alt"></i></button>
                        </form>
                    </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link waves-effect" href="/login" style="color: #fff;">Ingreso</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link waves-effect" href="/register" style="color: #fff;">Registro</a>
                        </li>
                        <% } %>
            </ul>

            <!-- Right -->
            <ul class="navbar-nav nav-flex-icons">
                <li class="nav-item">
                    <a class="nav-link waves-effect mr-2 pr-4" href="/cart ">
                        <i class="fas fa-shopping-cart" style="color: #fff;"></i>
                        <span class="clearfix d-none d-sm-inline-block" style="color: #fff;"> Carrito </span>
                        <span class="badge red z-depth-1 mr-1 cart-number">0</span>
                    </a>
                </li>
            </ul>

        </div>

    </div>
</nav>
<!-- Navbar -->